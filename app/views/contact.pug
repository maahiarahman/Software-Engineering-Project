extends layout

block content
  .py-20.bg-gradient-to-r.from-orange-500.to-amber-400.min-h-screen.flex.items-center.justify-center
    .bg-white.shadow-xl.rounded-2xl.p-10.w-full.max-w-2xl

      // 🌟 Header
      .text-center.mb-10
        h1.text-4xl.font-extrabold.text-orange-600.flex.items-center.justify-center.gap-2
          i.fa-solid.fa-envelope
          | Contact Us
        p.text-gray-700.mt-2.text-base Drop us a message — whether it’s a suggestion, recipe idea, or just to say hi!

      // 💬 Form
      form.contact-form(action="/contact", method="POST", id="contactForm")
        .mb-6
          label(for="name", class="block text-sm font-medium text-gray-700 mb-2") First Name
          div.relative
            input#name(type="text", name="name", required, placeholder="e.g. Taylor", class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-orange-500 focus:outline-none transition-all")

        .mb-6
          label(for="email", class="block text-sm font-medium text-gray-700 mb-2") Email
          div.relative
            input#email(type="email", name="email", required, placeholder="you@example.com", class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-orange-500 focus:outline-none transition-all")

        .mb-6
          label(for="message", class="block text-sm font-medium text-gray-700 mb-2") Message
          textarea#message(name="message", rows="5", required, placeholder="Type your message here...", class="w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-orange-500 focus:outline-none resize-none transition-all")

        // 🚀 Send Button
        button(type="submit", class="w-full py-3 mt-6 bg-orange-600 hover:bg-orange-700 text-white font-bold text-lg rounded-lg shadow-md transition-transform transform hover:scale-105 duration-200")
          i.fa-solid.fa-paper-plane.mr-2
          | Send Message
